# Lovable-specific configuration
# Lovable projects use Vite + React + Supabase

# Vite build creates static dist folder
ENV STATIC_BUILD=true

# Default Lovable port
ENV PORT=8080

# Lovable uses npm or yarn
RUN npm install -g serve

# Serve static files
CMD ["serve", "-s", "dist", "-l", "8080"]
