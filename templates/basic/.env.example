# Development Environment Variables for {{PROJECT_NAME}}
# Copy this file to .env and customize as needed

# Application Configuration
NODE_ENV=development
VITE_APP_TITLE={{PROJECT_NAME}}
VITE_API_BASE_URL=http://localhost:3000/api

# Vite Development Server Configuration
VITE_HOST=0.0.0.0
VITE_PORT=3000

# Docker Development Settings
CHOKIDAR_USEPOLLING=true

# Node.js Memory Settings (for large UI dependency trees)
NODE_OPTIONS=--max-old-space-size=4096

# Figma-specific Environment Variables
# Add any API keys or configuration needed for your Figma-exported components
# VITE_FIGMA_ACCESS_TOKEN=your_figma_token_here
# VITE_UNSPLASH_ACCESS_KEY=your_unsplash_key_here

# UI Library Configuration
# VITE_THEME_MODE=light
# VITE_DEFAULT_LOCALE=en-US

# Development Tools
# VITE_ENABLE_DEV_TOOLS=true
# VITE_SHOW_PERFORMANCE_METRICS=false

# Production Overrides (when NODE_ENV=production)
# VITE_API_BASE_URL=https://your-production-api.com
# VITE_CDN_URL=https://your-cdn.com

# Docker Compose Service Names (for internal networking)
# These are used when services need to communicate with each other
DEV_SERVICE_NAME={{PROJECT_NAME}}-dev
PROD_SERVICE_NAME={{PROJECT_NAME}}-prod
NETWORK_NAME={{PROJECT_NAME}}-network